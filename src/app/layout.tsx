import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "OWL-AGENT | Unified Synergy Gateway",
  description: "The ultimate free-tier AI proxy stack. One gateway for Hermes, Claude, OpenCode & Kiro. Download installers, explore architecture, and master the ecosystem.",
  keywords: ["OWL-AGENT", "AI Proxy", "Kiro Gateway", "Claude Max", "OpenCode", "Hermes", "Free Tier", "Proxy Defense", "MCP Server"],
  icons: {
    icon: "data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🦉</text></svg>",
  },
  openGraph: {
    title: "OWL-AGENT | Unified Synergy Gateway",
    description: "One gateway. All AI models. Zero cost.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground font-sans`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
